# Task

`lib/validate.sh`'s `is_positive` function incorrectly accepts `0` as
positive. Fix `is_positive` so it only accepts integers strictly greater
than zero. Do not modify `README.md` or the existing `is_blank` function.
