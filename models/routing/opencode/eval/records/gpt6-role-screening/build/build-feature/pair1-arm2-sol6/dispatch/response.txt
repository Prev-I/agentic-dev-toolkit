Using brainstorming to shape this small change. It looks bounded, so I’ll check the existing shell style and present a short design here before editing.
The existing `upper` function prints with `printf '%s\n'`, and the acceptance test expects `reverse abc` to produce `cba`. I’ll add `reverse` below `upper` in `lib/strings.sh`, using Bash string indexing to reverse the argument and the same print convention. Then I’ll run the acceptance and regression tests. Is that design okay?
I’ll add `reverse` below `upper` in `lib/strings.sh`, following its newline-printing convention, then run the acceptance and regression tests. **Do you approve this design?**