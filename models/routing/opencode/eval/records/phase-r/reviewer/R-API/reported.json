{
  "findings": [
    {
      "file": "api.sh",
      "severity": "blocking",
      "summary": "public_response interpolates its argument into JSON without escaping, so input containing a double quote can inject arbitrary JSON fields or emit invalid JSON."
    },
    {
      "file": "pagination.sh",
      "severity": "material",
      "summary": "validate_page_size accepts leading zeros in the regex but evaluates them as octal in the arithmetic test, so '08' errors and is rejected while '010' and '0100' are validated as 8 and 64."
    }
  ]
}
