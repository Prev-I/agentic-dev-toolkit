{
  "seeded": [
    {
      "id": "R-API",
      "severity": "none",
      "files": [
        "api.sh"
      ],
      "summary": "",
      "all_reported": [
        {
          "file": "pagination.sh",
          "severity": "material",
          "summary": "Leading zeros pass the digit regex and are then evaluated as octal in the arithmetic test, so \"0144\" is wrongly accepted (decimal 144 > 100) and \"09\" is wrongly rejected with an arithmetic error on stderr."
        },
        {
          "file": "authorization.sh",
          "severity": "suggestion",
          "summary": "An empty caller and empty owner compare equal and grant access, so the function should reject unpopulated identity arguments rather than rely on callers always supplying them."
        }
      ]
    },
    {
      "id": "R-AUTH",
      "severity": "blocking",
      "files": [
        "authorization.sh"
      ],
      "summary": "read_resource takes caller and owner but never compares them, returning the resource value to any caller regardless of ownership.",
      "all_reported": [
        {
          "file": "authorization.sh",
          "severity": "blocking",
          "summary": "read_resource takes caller and owner but never compares them, returning the resource value to any caller regardless of ownership."
        }
      ]
    },
    {
      "id": "R-BOUNDARY",
      "severity": "material",
      "files": [
        "pagination.sh"
      ],
      "summary": "validate_page_size accepts out-of-range values because (( )) reinterprets digit strings, so leading-zero inputs are read as octal (0144 passes as 100) and values above INTMAX wrap around (18446744073709551617 passes), bypassing the 0-100 bound.",
      "all_reported": [
        {
          "file": "pagination.sh",
          "severity": "material",
          "summary": "validate_page_size accepts out-of-range values because (( )) reinterprets digit strings, so leading-zero inputs are read as octal (0144 passes as 100) and values above INTMAX wrap around (18446744073709551617 passes), bypassing the 0-100 bound."
        }
      ]
    },
    {
      "id": "R-CONCURRENCY",
      "severity": "blocking",
      "files": [
        "counter.sh"
      ],
      "summary": "increment_counter performs an unsynchronized read-modify-write with no locking, so concurrent invocations lose updates (50 parallel increments yielded 3).",
      "all_reported": [
        {
          "file": "counter.sh",
          "severity": "blocking",
          "summary": "increment_counter performs an unsynchronized read-modify-write with no locking, so concurrent invocations lose updates (50 parallel increments yielded 3)."
        },
        {
          "file": "pagination.sh",
          "severity": "material",
          "summary": "Zero-padded input is evaluated as octal by (( )), so 0144 wrongly passes the <=100 bound while 08 raises an arithmetic error and rejects a valid size."
        },
        {
          "file": "authorization.sh",
          "severity": "suggestion",
          "summary": "With both arguments absent, caller and owner are both empty and compare equal, granting the read; an explicit non-empty owner check would fail closed."
        },
        {
          "file": "storage.sh",
          "severity": "suggestion",
          "summary": "The _or_fail name implies failure handling that is absent; it never verifies $storage is executable, so a missing backend surfaces only as exit 127."
        }
      ]
    },
    {
      "id": "R-ERROR",
      "severity": "material",
      "files": [
        "storage.sh"
      ],
      "summary": "Every backend failure is swallowed and converted into an empty JSON array with exit status 0, making an unreachable store indistinguishable from an empty one and risking data loss in read-modify-write callers.",
      "all_reported": [
        {
          "file": "authorization.sh",
          "severity": "material",
          "summary": "read_resource performs no arity or non-empty check, so an empty caller and empty owner compare equal and the function returns the value with success status, authorizing unauthenticated access."
        },
        {
          "file": "pagination.sh",
          "severity": "material",
          "summary": "The digit regex is followed by an arithmetic range check, so values above 2^64 wrap around and are accepted as valid page sizes while leading-zero inputs like 08 are parsed as octal and fail with a bash error."
        },
        {
          "file": "storage.sh",
          "severity": "material",
          "summary": "Every backend failure is swallowed and converted into an empty JSON array with exit status 0, making an unreachable store indistinguishable from an empty one and risking data loss in read-modify-write callers."
        },
        {
          "file": "counter.sh",
          "severity": "suggestion",
          "summary": "Locking and increment logic are correct, but non-numeric file content is silently coerced to 0, resetting the counter instead of reporting corruption."
        },
        {
          "file": "api.sh",
          "severity": "suggestion",
          "summary": "JSON encoding is safe, but a missing argument silently yields an empty displayName instead of an error."
        }
      ]
    }
  ],
  "clean": [
    {
      "severity": "suggestion",
      "file": "pagination.sh",
      "summary": "Leading-zero input is evaluated as octal in the arithmetic context, so 007 is accepted as 7 while 08 is rejected with a stderr error; failing closed is safe but inconsistent, and prefixing with 10# would normalize it."
    }
  ],
  "normalization": "highest severity the reviewer reported against the case's override file",
  "runner_decides_gate_outcome": false,
  "scorer": "eval/scoring/reviewer.sh::reviewer_structured_gate"
}
